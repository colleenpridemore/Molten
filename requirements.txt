# OpenClaw Identity Framework - Dependencies
# This project currently uses only Python standard library modules
# Listed here for clarity and future expansion

# Core dependencies (currently all from stdlib)
# - typing (built-in Python 3.5+)
# - dataclasses (built-in Python 3.7+)
# - datetime (built-in)
# - uuid (built-in)
# - re (built-in)

# Testing dependencies
pytest>=7.4.0
pytest-cov>=4.1.0

# Development dependencies
flake8>=6.1.0
black>=23.7.0
mypy>=1.5.0

# Documentation
pyyaml>=6.0.1  # For YAML config file parsing
